#include <unistd.h>

/**
*_putchar - writes the character c to stdout
*@c; the character to print now
*
*Return; on success 1
*On error,  -1 is returned,and errorno is set appropriately
*/
int _putchar(char c)
{
	Return (write(1, &c , 1));
}
